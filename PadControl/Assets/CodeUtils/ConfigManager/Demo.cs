using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Demo : MonoBehaviour
{
    // Start is called before the first frame update
    private void Start()
    {
        //图形设置
        //string quality = Settings.ini.Graphics.Quality;
        //Settings.ini.Graphics.Quality = "High";

        //var (width, height) = Settings.ini.Graphics.Resolution;

        //Debug.Log(width);

        //// 游戏设置
        //string difficulty = Settings.ini.Game.Difficulty;
        //Settings.ini.Game.Difficulty = ("Normal");

        //string language = Settings.ini.Game.Language;
        //Settings.ini.Game.Language = ("zh");

        //Settings.ini.Graphics.Resolution = (1920, 1080);
    }

    // Update is called once per frame
    private void Update()
    {
    }
}